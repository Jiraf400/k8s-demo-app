apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jirafus400
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /cats
            pathType: Prefix
            backend:
              service:
                name: cats-api
                port:
                  number: 80